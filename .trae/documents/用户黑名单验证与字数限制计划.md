# 用户黑名单验证与评论字数限制计划

## 当前状态分析

### 帖子信息确认

| 帖子ID | 发帖人 | 状态记录 |
|--------|--------|----------|
| 77097 | u3001032032392137（牙牙） | state.json 已记录（第32-33行） |
| 82221 | u3001032032392137（牙牙） | state.json 已记录（第56-57行） |

### 用户黑名单当前状态

[user_blacklist.json](file:///c:/Users/fffhy/Desktop/自动化/user_blacklist.json) 当前只有：
- qiaobusen2026
- 用户64375

**问题**：`u3001032032392137`（牙牙）不在黑名单中，但其帖子已被评价。

### 原因分析

从执行日志可以推断：
- 之前的测试运行中，77097 和 82221 都被处理过
- 但当时用户黑名单功能可能还未完善，或者黑名单被清空了
- 现在 user_blacklist.json 只有测试时添加的两个用户

### 验证方案

由于两个帖子都已评价过，测试场景改为：
1. 清空 state.json 中这两个帖子的记录
2. 手动将 `u3001032032392137` 加入 user_blacklist.json
3. 运行脚本，验证：
   - 77097 → 应显示"用户黑名单跳过"
   - 82221 → 应显示"用户黑名单跳过"

或者采用反向验证：
1. 清空两个帖子的 state 记录
2. 不将用户加入黑名单
3. 运行脚本处理 77097（会投票+评论）
4. 验证 `u3001032032392137` 自动加入黑名单
5. 后续遇到 82221 时自动跳过

## 评论字数限制修改

### 当前状态

[comment_generator.py](file:///c:/Users/fffhy/Desktop/自动化/comment_generator.py#L10-L12) 当前规则：
```
1. 必须以'已投票'开头
2. 总字数不超过 50 字
3. 内容简短、真诚、有互动感
```

**缺失**：没有"至少15字"的要求

### 修改方案

修改 USER_PROMPT_TEMPLATE，添加字数限制要求：
```
"1. 必须以'已投票'开头\n"
"2. 总字数在 15-50 字之间\n"  ← 修改这里
"3. 内容简短、真诚、有互动感\n\n"
```

同时在 `generate` 方法中添加字数检查：
- 如果评论少于15字，补充内容使其达到15字
- 如果超过50字，截断到50字

## 实施步骤

### 验证用户黑名单功能

**方案选择**：采用反向验证（更直观）

1. 从 state.json 中移除 77097 和 82221 的记录
2. 清空 user_blacklist.json
3. 运行脚本，设置 MAX_COMMENTS_PER_RUN=2 只处理这两个帖子
4. 观察日志：
   - 77097 → 投票+评论成功 → 用户自动加入黑名单
   - 82221 → 用户黑名单跳过

### 修改评论字数限制

1. 编辑 [comment_generator.py](file:///c:/Users/fffhy/Desktop/自动化/comment_generator.py)
2. 修改 USER_PROMPT_TEMPLATE 第2条要求
3. 在 generate 方法中添加最少15字的检查逻辑

## 预期效果

### 用户黑名单验证

运行脚本后：
```
[1] 主题 77097 - 游之锚——游戏运营情感化召回工具...
  [用户] 发帖人: u3001032032392137
  [投票] 点击投票按钮
  [评论] ...
  [用户] 已将 u3001032032392137 加入用户黑名单
  [结果] 处理成功

[2] 主题 82221 - 游之锚——游戏运营情感化召回工具...
  用户黑名单跳过 (u3001032032392137)
```

### 评论字数限制

生成的评论长度在 15-50 字之间，例如：
- "已投票！这个工具很实用。" → 补充为 "已投票！这个工具很实用，值得一试。"
- "已投票，很棒！" → 补充为 "已投票，很棒！期待更多作品。"

## 需要确认的问题

验证用户黑名单功能时，需要临时清空 state.json 和 user_blacklist.json 的相关记录，这会影响当前的持久化状态。是否同意进行此操作？